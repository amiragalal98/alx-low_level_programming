#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
/**
 * constant = fixed value
 * that can not be altered by
 * the program during its execution
 */
	const float pi = 3.14;

	printf("pi equal: %.2f", pi);
	return (0);
}
