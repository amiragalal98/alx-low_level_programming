#include <stdio.h>
/**
 * main -entry point
 *
 * Return: Always 0(success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/* return 0 success */
	return (0);
	/**
	 * return :always 0(success)
	 */
}
