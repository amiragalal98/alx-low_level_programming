#include <stdio.h>
#define PI 3.14
/**
 * there are two way
 * to define constant value
 * define
 * const
 * define constant using define
 * const to define constant
 * Main -  Entry point
 * Return: return 0 (success)
 */
int main(void)
{
	const int i = 0;

	printf("the value of pi is: %.2f\n", PI);
	printf("the value of i is: %d", i);
	return (0);
}


